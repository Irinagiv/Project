@model IEnumerable<ElcutCRM.Data.Models.History>
@using ElcutCRM.Helpers;
@if (Model.Count() > 0)
{
    <div class="row">
        <div class="span6">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>
                            Событие
                        </th>
                        <th>
                            Дата
                        </th>
                        <th>
                        </th>
                    </tr>
                </thead>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @item.Event.Name
                        </td>
                        <td>@item.EventDate.ToString()
                        </td>
                        <td>
                            <div class="btn-group">
                                <a class="btn btn-primary dropdown-toggle" data-toggle="dropdown" href="#">Действие
                                    <span class="caret"></span></a>
                                <ul class="dropdown-menu">
                                    <li>@Ajax.ActionLink("Удалить", "DeleteHistory", new { id = item.ID }, new AjaxOptions
                                   {
                                       HttpMethod = "POST",
                                       Confirm = "Удалить историю?",
                                       UpdateTargetId = "historyContainer"
                                   })</li>
                                </ul>
                            </div>
                        </td>
                    </tr>
                }
            </table>
            <hr />
        </div>
    </div>
}